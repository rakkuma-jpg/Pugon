<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PugonPrototype.Views.ChatbotView">
    <Border BackgroundColor="Transparent"
            Stroke="Transparent"
            StrokeThickness="0">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="20,20,20,20" />
        </Border.StrokeShape>
        <VerticalStackLayout HorizontalOptions="Fill" BackgroundColor="LightGray">
            <Label 
                Text="this is chatbot page"
                TextColor="Black"
                FontSize="Header"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />
        </VerticalStackLayout>
    </Border>
</ContentView>
